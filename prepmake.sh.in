#!/bin/bash

if [ "${GEN}" = "" ]; then
    GEN='Unix Makefiles'
fi

@USE_LIBFIREBREATH_STR@

source ${0%/*}/common.sh "$@"

pushd "$BUILDDIR"
cmake -G "$GEN" -DFB_PROJECTS_DIR="${PROJDIR}" @BOOST_FLAG@ @SHARED_FLAG@ "$@" "${FB_ROOT}"
popd
